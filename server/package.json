{
  "name": "@drawbridge/server",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../.env.local --env-file-if-exists=../.env src/index.ts",
    "start": "node --env-file-if-exists=../.env.local --env-file-if-exists=../.env src/index.ts",
    "test": "node --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^8.0.4",
    "fastify": "^5.2.1",
    "undici": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
